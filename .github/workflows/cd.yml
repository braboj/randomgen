# Continuous delivery: on a release tag, record a GitHub Release, publish the
# Docker image to Docker Hub, then trigger the Render redeploy. One
# responsibility per job — record the release, publish the artifact, then
# deploy it — with deploy gated on a successful publish. Tag-triggered only.
#
# This workflow uses a third-party action (docker-build-push). Actions are
# pinned by commit SHA (Dependabot keeps the pins current); the trailing
# comment records the human-readable version.

name: CD

on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

permissions:
  contents: read

jobs:
  release:
    name: Record GitHub Release
    # Creates a GitHub Release for the pushed tag with auto-generated notes
    # (the "What's Changed" PR list + a full changelog link). Independent of the
    # artifact jobs — the Release records the source at the tag, so a publish
    # hiccup must not erase the release record. Tag pushes only: a manual
    # workflow_dispatch runs on a branch, which has no release tag.
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Create the GitHub Release (idempotent)
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GH_REPO: ${{ github.repository }}
        run: |
          if gh release view "${GITHUB_REF_NAME}" >/dev/null 2>&1; then
            echo "Release ${GITHUB_REF_NAME} already exists; skipping."
          else
            gh release create "${GITHUB_REF_NAME}" \
              --title "${GITHUB_REF_NAME}" \
              --generate-notes \
              --verify-tag
          fi

  publish:
    name: Publish image to Docker Hub
    runs-on: ubuntu-latest
    steps:
      - name: Check out the repo
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7

      - name: Build and push Docker image
        uses: mr-smithers-excellent/docker-build-push@ef432483bd0252dd948d1d11e60c96a1c93646a9  # v7.0
        with:
          image: braboj/randomgen
          registry: docker.io
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
          addLatest: true
          pushImage: true

  scan:
    name: Scan image and emit SBOM
    # Runs after the image is published. Advisory only: it is NOT a dependency
    # of `deploy`, and `continue-on-error` keeps a scan/SBOM hiccup (or an
    # unpatchable upstream-base CVE) from ever blocking the release.
    needs: publish
    runs-on: ubuntu-latest
    continue-on-error: true
    permissions:
      # write so the advisory job can attach the SBOM to the tag's Release.
      contents: write
    steps:
      - name: Check out the repo
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7

      - name: Scan the published image (Trivy, advisory)
        uses: aquasecurity/trivy-action@ed142fd0673e97e23eac54620cfb913e5ce36c25  # v0.36.0
        with:
          image-ref: docker.io/braboj/randomgen:latest
          format: table
          vuln-type: os,library
          severity: HIGH,CRITICAL
          # Advisory: report, never fail (the base image may carry unpatched CVEs).
          exit-code: '0'

      - name: Scan the Dockerfile config (Trivy, advisory)
        uses: aquasecurity/trivy-action@ed142fd0673e97e23eac54620cfb913e5ce36c25  # v0.36.0
        with:
          scan-type: config
          scan-ref: Dockerfile
          severity: HIGH,CRITICAL
          exit-code: '0'

      - name: Generate the image SBOM (Syft)
        uses: anchore/sbom-action@e22c389904149dbc22b58101806040fa8d37a610  # v0.24.0
        with:
          image: docker.io/braboj/randomgen:latest
          format: spdx-json
          output-file: randomgen.sbom.spdx.json
          # Generate the file only. The action's own auto-uploads default to true
          # and otherwise double-emit the SBOM (a `*_latest.spdx.json` workflow
          # artifact + Release asset) alongside the two explicit steps below; the
          # explicit steps own both uploads, with controlled, version-stable names.
          upload-artifact: false
          upload-release-assets: false

      - name: Upload the SBOM as a build artifact
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1
        with:
          name: sbom-spdx
          path: randomgen.sbom.spdx.json
          if-no-files-found: error

      - name: Attach the SBOM to the GitHub Release (advisory)
        # On a tag push, attach the SBOM to the tag's Release as a durable asset
        # so each published version carries its SBOM alongside the notes — the
        # build artifact above expires with the run's retention (AD-29/AD-30).
        # The Release is created by the independent `release` job; this step
        # waits briefly for it (publish + scan normally outlast the release
        # create) and is guarded so a missing Release or SBOM never fails this
        # advisory job — keeping the Release record off the scan path (AD-30).
        # --clobber makes a re-run idempotent.
        if: startsWith(github.ref, 'refs/tags/v')
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GH_REPO: ${{ github.repository }}
        run: |
          if [ ! -f randomgen.sbom.spdx.json ]; then
            echo "SBOM not found; skipping Release upload." >&2
            exit 0
          fi
          for attempt in 1 2 3 4 5; do
            if gh release view "${GITHUB_REF_NAME}" >/dev/null 2>&1; then
              gh release upload "${GITHUB_REF_NAME}" randomgen.sbom.spdx.json --clobber
              echo "Attached SBOM to Release ${GITHUB_REF_NAME}."
              exit 0
            fi
            echo "Release ${GITHUB_REF_NAME} not present yet (attempt ${attempt}); waiting..."
            sleep 10
          done
          echo "Release ${GITHUB_REF_NAME} did not appear; SBOM remains a build artifact only." >&2

  deploy:
    name: Trigger Render redeploy
    # Only deploy an image that was successfully published (artifact promotion).
    needs: publish
    runs-on: ubuntu-latest
    steps:
      # Tell Render to pull the freshly pushed image and redeploy. The hook URL
      # is a per-service secret (RENDER_DEPLOY_HOOK_URL); if it is unset the step
      # is skipped so the workflow still succeeds.
      - name: Trigger Render deploy
        env:
          RENDER_DEPLOY_HOOK_URL: ${{ secrets.RENDER_DEPLOY_HOOK_URL }}
        run: |
          if [ -n "$RENDER_DEPLOY_HOOK_URL" ]; then
            echo "Triggering Render deploy..."
            curl -fsS -X POST "$RENDER_DEPLOY_HOOK_URL"
          else
            echo "RENDER_DEPLOY_HOOK_URL not set; skipping Render deploy." >&2
          fi
