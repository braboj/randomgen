# Extra dependencies for the dev/demo helper scripts. The scripts that drive
# the app (e.g. capture_ui_snapshots.py) import the installed `randomgen`
# package and get Flask transitively from it, so Flask is not duplicated here.
numpy~=2.5.2
matplotlib~=3.11.1
requests~=2.34.2
